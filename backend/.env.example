# Django Secret Key (Don't use the real one here)
SECRET_KEY=your-secret-key-placeholder

# Database Config (Postgres)
DB_NAME=portfolio_db
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# Debug Mode
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
